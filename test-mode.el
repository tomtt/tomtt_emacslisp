(defvar test-mode-abbrev-table nil
  "Abbrev table in use in test-mode buffers.")

(define-abbrev-table 'test-mode-abbrev-table ())

(defvar test-mode-map nil "Keymap used in test mode.")

(provide 'test-mode)
